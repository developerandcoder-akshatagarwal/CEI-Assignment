Week 8 Assignment: Single Agent System

This project implements a simple AI agent that uses conditional routing to select and execute appropriate tools based on user queries. The agent supports basic calculations, keyword extraction, and text statistics generation. It returns structured JSON responses and includes logging and error handling for improved reliability.
